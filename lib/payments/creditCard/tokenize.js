/* LIB */

const radial = require('../../../radial');
const utils = require('../../utils');

/* MODULES */

/* CONSTRUCTOR */

(function () {

  var Tokenize = {};

  /* PRIVATE VARIABLES */

  /* PUBLIC VARIABLES */

  /* PUBLIC FUNCTIONS */

  Tokenize.execute = function (options, fn) {

  };

  /* NPM EXPORT */

  if (typeof module === 'object' && module.exports) {
    module.exports = Tokenize.execute;
  } else {
    throw new Error('This module only works with NPM in NodeJS environments.');
  }

}());
